const template = `
<h1>Definitions</h1>
<h2>Algorithmic composition</h2>
<ul>
<li>Translation: e.g. image to music</li>
<li>Mathematical models: e.g. randomization, fractals</li>
<li>Knowledge-based: define sets of rules</li>
<li>Machine learning</li>
</ul>

My music generator: generate continuous real-time music based on pre-defined rules, procedures and randomization without (or with minimal) input in a browser
`;

const entry = () => {};
const exit = () => {};

export { template, entry, exit };
